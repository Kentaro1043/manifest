apiVersion: apps/v1
kind: Deployment
metadata:
  name: misskey
  labels:
    app: misskey
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  selector:
    matchLabels:
      app: misskey
  template:
    metadata:
      labels:
        app: misskey
    spec:
      containers:
        - name: misskey
          image: docker.io/misskey/misskey:2025.12.1@sha256:60e9002f2bdea5aa8fa370cc98db41e5c195ba499c1d56bf06c5fcf6880e8df2
          volumeMounts:
            - name: misskey-config
              mountPath: /misskey/.config
              readOnly: true
          ports:
            - containerPort: 3000
          resources:
            limits:
              cpu: "200m"
              memory: "512Mi"
            requests:
              cpu: "100m"
              memory: "256Mi"
      volumes:
        - name: misskey-config
          secret:
            secretName: misskey-config
