apiVersion: v1
kind: Service
metadata:
  name: minecraft-be
  annotations:
    external-dns.alpha.kubernetes.io/target: "oci-lb.kentaro1043.com"
    external-dns.alpha.kubernetes.io/hostname: "sakura-mc.kentaro1043.com"
    external-dns.alpha.kubernetes.io/cloudflare-proxied: "false"
spec:
  selector:
    app: minecraft-be
  type: ClusterIP
  externalTrafficPolicy: Local
  ports:
    - port: 19132
      protocol: UDP
