helmCharts:
  - name: traefik
    repo: https://traefik.github.io/charts
    version: 35.4.0
    releaseName: traefik
    namespace: traefik
    includeCRDs: true
    additionalValuesFiles:
      - ./additional-values.yaml

resources:
  - ./certificate.yaml
